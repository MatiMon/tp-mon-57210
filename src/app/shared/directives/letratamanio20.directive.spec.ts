import { Letratamanio20Directive } from './letratamanio20.directive';

describe('Letratamanio20Directive', () => {
  it('should create an instance', () => {
    const directive = new Letratamanio20Directive();
    expect(directive).toBeTruthy();
  });
});
